```json
{
  "success": false,
  "output": "The `run_code` tool is consistently failing with a 'Tool not found' error. This prevents any code from being executed, including diagnostic scripts intended to debug previous failures. After multiple retries, it is evident that this is a persistent environment or tool configuration issue that cannot be resolved by resubmitting the code. The last attempted action was to execute a diagnostic script to inspect the contents of the `/work_dir/data` directory and the headers of the `cash_flows.csv` file.",
  "attempts": 3
}
```